#include "main.h"

/**
 * _strlen -> The length of a string
 *
 * @s: Input
 *
 * Return: i String Length
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	return (i);
}
